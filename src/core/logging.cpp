#include "core/logging.h"

// Logging implementation is header-only (inline functions).
// This file exists to satisfy the CMake source list and
// provide a place for future non-inline logging utilities
// (e.g., file logging, structured output).

namespace titan {

// Placeholder for future file-based logging
// void init_file_logging(const std::string& path) { ... }

} // namespace titan
